
#include <iostream>
using namespace std;
int main()
{
    int base , height;
    cout << "Input the base of triangle in meters" << endl;
    cin >> base;
    cout << "Input the height of triangle in  meters" << endl;
    cin >> height;
    cout << "Area of triangle is " << (0.5 * height * base) << " meters" << endl;
    return 0;
}